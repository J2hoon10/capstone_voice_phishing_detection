# AIShield 포스터용 정리본

## Abstract

보이스피싱은 정상 번호 도용, 기관 사칭, 심리적 압박, 딥페이크 음성 등으로 빠르게 고도화되고 있으며, 기존의 블랙리스트 기반 차단이나 URL 탐지 방식만으로는 대응에 한계가 있다. 특히 피해는 통화가 끝난 뒤가 아니라 통화 도중 발생하는 경우가 많아, 사후 분석보다 실시간 개입형 탐지 시스템의 필요성이 크다.

본 프로젝트는 통화 음성을 STT로 텍스트화한 뒤, 대화의 문맥을 분석하여 보이스피싱 위험과 유형을 분류하고, 사용자에게 즉시 대응 가이드를 제공하는 AI 기반 시스템을 제안한다. 이를 위해 KoELECTRA 기반 인코더와 Mamba SSM 기반 순차 모델링 구조를 결합한 Streaming Belief v5 분류기를 사용하였다.

실험 결과 validation macro F1은 0.8316, test macro F1은 0.7627을 기록하였으며, 특히 수사기관 사칭형과 같은 고위험 범주에서 비교적 높은 탐지 성능을 확인하였다. 본 연구는 보이스피싱을 사후 판별하는 데 그치지 않고, 통화 중 위험을 감지하고 대응을 유도하는 보호형 AI 시스템의 가능성을 보여준다.

## Data

본 실험은 음성 전사문 기반 텍스트 분류 문제로 구성되었으며, 학습 데이터는 `train.csv`, 검증 데이터는 `val.csv`, 테스트 데이터는 `test.csv`를 사용하였다. 분류 대상은 다음 5개 클래스이다.

- 상품 가입 및 해지
- 이체 출금 대출서비스
- 잔고 및 거래내역
- 수사기관 사칭형
- 대출 사기형

이 중 수사기관 사칭형과 대출 사기형은 실제 보이스피싱과 직접적으로 연관된 고위험 범주이며, 나머지 클래스는 정상 금융 상담 및 일반 문의와의 구분을 위한 비교 범주로 활용하였다.

참고 보고서에서도 확인되듯 보이스피싱 데이터는 일반 데이터 대비 양이 적고, 특정 유형에 편중되어 있어 클래스 불균형과 일반화 성능 저하 문제가 존재한다. 따라서 본 프로젝트는 데이터 증강, STT 오차 반영 전처리, 클래스 불균형 완화 등을 향후 핵심 개선 요소로 설정하였다.

## Method

본 시스템은 다음과 같은 파이프라인으로 동작한다.

`음성 입력 -> STT 변환 -> 텍스트 전처리 -> 문맥 기반 유형 분류 -> 대응 가이드 제공`

모델은 긴 통화 문맥을 처리하기 위해 텍스트 전체를 한 번에 입력하지 않고, 일정 길이의 세그먼트로 분할하여 순차적으로 해석하도록 설계하였다.

핵심 구조는 다음과 같다.

- `KoELECTRA`: 각 텍스트 세그먼트의 의미를 인코딩
- `Attention-weighted pooling`: 세그먼트 대표 벡터 생성
- `Mamba SSM`: 세그먼트 간 흐름을 따라 문맥을 누적
- `Classification Head`: 최종 피싱 유형 예측

이 구조는 통화 후반부에 등장하는 위험 발화도 흐름 속에서 반영할 수 있다는 장점이 있다. 또한 본 프로젝트는 장기적으로 온디바이스 환경을 고려하고 있으며, 개인정보 유출을 줄이고 실시간성을 높이기 위한 경량화 및 지연 시간 최적화를 향후 과제로 두고 있다.

실험 설정은 다음과 같다.

- 배치 크기: 8
- 학습 epoch: 8
- 최적 모델 선택 기준: validation macro F1
- 최적 checkpoint: epoch 6

## Result

### Validation Best

- Best epoch: 6
- Validation accuracy: 0.8484
- Validation macro F1: 0.8316
- Validation weighted F1: 0.8484

### Test Performance

- Test accuracy: 0.7607
- Test macro F1: 0.7627
- Test weighted F1: 0.7616

### Class-wise Test F1

- 상품 가입 및 해지: 0.8117
- 이체 출금 대출서비스: 0.6286
- 잔고 및 거래내역: 0.7869
- 수사기관 사칭형: 0.8551
- 대출 사기형: 0.7312

결과적으로 수사기관 사칭형에서 가장 높은 탐지 성능을 보였고, 대출 사기형에서도 비교적 안정적인 예측이 가능했다. 반면 이체 출금 대출서비스는 정상 금융 상담과 표현이 겹치는 경우가 많아 상대적으로 낮은 성능을 보였다.

또한 validation 대비 test 성능이 하락한 것으로 보아, 현재 모델은 데모 수준에서는 유의미한 가능성을 보였지만 일반화 성능 측면에서는 추가 개선이 필요하다.

## Conclusion

본 프로젝트는 보이스피싱 대응의 핵심을 기존의 번호·패턴 기반 차단에서 대화 문맥 기반의 실시간 위험 탐지로 전환하고자 하였다. 이를 위해 STT 기반 텍스트 전사와 KoELECTRA + Mamba 기반 문맥 모델링을 결합하였고, 실제 실험에서 고위험 범주에 대해 의미 있는 분류 성능을 확인하였다.

특히 본 연구의 의의는 단순 분류 정확도에만 있지 않다. 보이스피싱을 통화가 끝난 뒤 판별하는 것이 아니라, 통화 도중 위험을 감지하고 사용자가 즉시 대응할 수 있도록 돕는 보호형 AI 시스템의 가능성을 제시했다는 점에 있다.

향후에는 다음 방향으로 발전시킬 수 있다.

- 데이터 증강 및 클래스 불균형 보완
- 오분류 분석 기반 모델 개선
- 실시간 스트리밍 환경 latency 최적화
- 조기 탐지 성능 강화
- 경량화 및 온디바이스 배포 검증

궁극적으로 본 시스템은 디지털 취약계층 보호, 금융기관 및 통신사의 예방 서비스, 개인정보 보호를 고려한 온디바이스 보안 AI로 확장될 가능성을 가진다.
