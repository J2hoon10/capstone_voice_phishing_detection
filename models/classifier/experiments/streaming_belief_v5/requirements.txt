'''
# 1. torch 등 기본 라이브러리 및 빌드 도구를 먼저 설치
pip install torch>=2.0.0 transformers>=4.30.0 numpy>=1.24.0 scikit-learn>=1.3.0 peft>=0.11.0
pip install packaging ninja wheel
# 2. causal-conv1d 및 mamba-ssm 은 반드시 의존성 빌드 도구가 깔린 후에 마지막으로 설치
pip install causal-conv1d>=1.2.0
pip install mamba-ssm>=1.2.0
'''

torch>=2.0.0
transformers>=4.30.0
numpy>=1.24.0
scikit-learn>=1.3.0
peft>=0.11.0

# Linux(CUDA) 환경에서 mamba-ssm 컴파일 및 설치를 위한 필수 패키지
packaging
ninja
wheel
mamba-ssm>=1.2.0
causal-conv1d>=1.2.0
