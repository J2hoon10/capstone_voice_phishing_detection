# 캡스톤디자인 데모데이 포스터 발표 초안

## 1. 작품명

**실시간 음성 피싱 탐지 및 대응 가이드 시스템**

부제:
통화 음성을 실시간으로 분석하여 보이스피싱 위험을 분류하고, 사용자에게 즉시 대응 방법을 제공하는 AI 기반 데모 시스템

---

## 2. 문제 정의

보이스피싱은 여전히 대표적인 금융 범죄 중 하나이며, 피해자는 통화 중 심리적으로 압박을 받는 상황에서 합리적인 판단을 내리기 어렵다.  
기존 대응 방식은 주로 사후 신고 또는 정적 안내에 머무르기 때문에, 실제 통화 중 위험을 조기에 감지하고 즉시 경고하는 시스템의 필요성이 크다.

본 프로젝트는 이 문제를 해결하기 위해 다음 목표를 가진다.

- 통화 음성을 기반으로 피싱 위험을 자동 분류한다.
- 실시간 또는 준실시간으로 결과를 사용자에게 전달한다.
- 단순 탐지에서 끝나지 않고, 상황별 대응 가이드를 함께 제공한다.

---

## 3. 개발 목표

- 음성 입력을 텍스트로 변환하는 전처리 파이프라인 구축
- 피싱 유형을 분류하는 딥러닝 기반 분류기 개발
- 탐지 결과에 따라 대응 문구를 제공하는 가이드 모듈 구현
- 백엔드와 프론트엔드를 포함한 데모 가능한 통합 시스템 완성

---

## 4. 시스템 개요

본 시스템은 크게 4단계로 동작한다.

1. 사용자의 통화 음성 또는 오디오 입력을 수집한다.
2. 음성 인식 모델이 발화를 텍스트로 변환한다.
3. 텍스트 분류 모델이 보이스피싱 유형을 예측한다.
4. 예측 결과에 따라 사용자에게 위험 유형과 대응 가이드를 제공한다.

요약 흐름:

`음성 입력 -> STT(Whisper 계열) -> 텍스트 분류기 -> 피싱 유형 예측 -> 대응 가이드 제공`

---

## 5. 사용 기술

### AI / 모델링

- Whisper 계열 음성 인식 모델
- KoELECTRA 기반 텍스트 인코더
- Streaming Belief v5 분류기
- Mamba SSM 기반 시퀀스 모델링

### 서비스 / 시스템

- FastAPI
- WebSocket 기반 스트리밍 API
- React + Vite 프론트엔드
- Docker 기반 통합 실행 환경

---

## 6. 모델 설계 아이디어

이번 분류기는 긴 통화 내용을 한 번에 처리하지 않고, 일정 길이의 세그먼트로 나누어 순차적으로 해석하도록 설계하였다.

핵심 아이디어는 다음과 같다.

- KoELECTRA가 각 텍스트 세그먼트의 의미를 인코딩한다.
- Attention-weighted pooling으로 세그먼트 대표 벡터를 만든다.
- Mamba SSM이 세그먼트들의 흐름을 따라가며 문맥을 누적한다.
- 최종적으로 누적된 표현을 이용해 피싱 유형을 분류한다.

이 구조는 긴 통화에서 중요한 문맥이 뒤쪽에 등장하더라도, 순차적으로 의미를 반영할 수 있다는 장점이 있다.

---

## 7. 분류 대상

본 실험에서는 다음 5개 클래스로 유형을 분류하였다.

- 상품 가입 및 해지
- 이체 출금 대출서비스
- 잔고 및 거래내역
- 수사기관 사칭형
- 대출 사기형

이 중 `수사기관 사칭형`, `대출 사기형`은 실제 보이스피싱과 직접적으로 연관된 고위험 범주이며, 나머지 클래스는 정상 상담 또는 일반 금융 문의와 구분하기 위한 비교 범주로 활용하였다.

---

## 8. 실험 설정

- 학습 데이터: `train.csv`
- 검증 데이터: `val.csv`
- 테스트 데이터: `test.csv`
- 배치 크기: 8
- 학습 epoch: 8
- 입력 처리: 세그먼트 윈도우 기반 분할
- 최적 모델 선택 기준: validation macro F1

실험 결과 최고 성능은 **epoch 6**에서 기록되었으며, 해당 체크포인트를 최종 평가에 사용하였다.

---

## 9. 실험 결과

### Validation 최고 성능

- Best epoch: 6
- Validation accuracy: `0.8484`
- Validation macro F1: `0.8316`
- Validation weighted F1: `0.8484`

### Test 성능

- Test accuracy: `0.7607`
- Test macro F1: `0.7627`
- Test weighted F1: `0.7616`

### 클래스별 Test F1

- 상품 가입 및 해지: `0.8117`
- 이체 출금 대출서비스: `0.6286`
- 잔고 및 거래내역: `0.7869`
- 수사기관 사칭형: `0.8551`
- 대출 사기형: `0.7312`

### 결과 해석

- `수사기관 사칭형`에서 가장 높은 성능을 보였으며, 위험 시나리오 식별 가능성을 확인하였다.
- `대출 사기형` 또한 비교적 안정적으로 탐지하였다.
- `이체 출금 대출서비스` 클래스는 다른 클래스와 표현이 겹치는 구간이 있어 상대적으로 낮은 성능을 보였다.
- Validation 대비 Test 점수가 다소 하락하여, 추가적인 데이터 보강과 일반화 성능 개선이 필요함을 확인하였다.

---

## 10. 기대 효과

- 실제 통화 중 위험 발화를 빠르게 감지하여 사용자 피해를 줄일 수 있다.
- 고령층 등 디지털 취약계층에게 실시간 보조 도구로 활용 가능하다.
- 금융기관, 통신사, 공공기관의 보이스피싱 예방 서비스로 확장 가능하다.

---

## 11. 한계 및 개선 방향

### 현재 한계

- 테스트 성능이 검증 성능보다 낮아 일반화 성능 개선이 필요함
- 특정 클래스 간 표현 중복으로 인해 오분류가 발생함
- 실제 통화 환경의 잡음, 방언, 끊김 등에 대한 추가 검증이 필요함

### 향후 개선 방향

- 데이터 증강 및 클래스 불균형 보완
- confusion matrix 기반 오분류 분석 고도화
- 실시간 스트리밍 환경에서의 latency 최적화
- 탐지 결과 신뢰도와 대응 가이드의 개인화 강화

---

## 12. 포스터용 한 줄 메시지

**“통화가 끝난 뒤 대응하는 것이 아니라, 통화 중에 위험을 감지하고 바로 대응할 수 있도록 돕는 보이스피싱 탐지 시스템”**

---

## 13. 발표 시 1분 설명 스크립트

안녕하세요. 저희 팀은 실시간 음성 피싱 탐지 및 대응 가이드 시스템을 개발했습니다.  
이 프로젝트는 사용자의 통화 음성을 텍스트로 변환한 뒤, AI 분류 모델이 보이스피싱 유형을 판별하고, 그 결과에 맞는 대응 가이드를 즉시 제공하는 것이 핵심입니다.

저희는 긴 통화 문맥을 처리하기 위해 KoELECTRA와 Mamba 기반의 Streaming Belief 구조를 적용했습니다.  
이를 통해 통화 내용을 여러 세그먼트로 나누어 순차적으로 이해하고, 최종적으로 피싱 유형을 분류할 수 있도록 설계했습니다.

실험 결과 validation macro F1은 0.8316, test macro F1은 0.7627을 기록했고, 특히 수사기관 사칭형 탐지에서 높은 성능을 확인했습니다.  
향후에는 데이터 확장과 실시간 최적화를 통해 실제 서비스 수준으로 고도화하는 것이 목표입니다.

---

## 14. 발표 시 3분 설명 스크립트

안녕하세요. 저희 팀의 주제는 실시간 음성 피싱 탐지 및 대응 가이드 시스템입니다.

보이스피싱은 단순히 사후에 녹취를 분석하는 것만으로는 피해를 막기 어렵습니다. 피해자는 통화 중 심리적으로 위축되거나 압박을 받기 때문에, 실제로는 통화 도중에 위험을 감지하고 즉시 알려주는 방식이 훨씬 중요합니다. 저희는 이 문제를 해결하기 위해 음성 인식, 텍스트 분류, 대응 가이드 제공을 하나의 흐름으로 연결한 데모 시스템을 만들었습니다.

시스템은 먼저 음성을 입력받고, 이를 Whisper 계열 모델로 텍스트로 변환합니다. 이후 텍스트 분류기가 통화 내용을 분석하여 어떤 유형의 발화인지 예측합니다. 마지막으로 위험 유형에 따라 사용자에게 대응 방법을 안내합니다.

모델 측면에서는 긴 통화 문맥을 처리하는 것이 중요했기 때문에, KoELECTRA 기반 인코더와 Mamba SSM 기반 구조를 결합한 Streaming Belief v5 실험 모델을 사용했습니다. 긴 텍스트를 작은 세그먼트로 나누고, 각 세그먼트 의미를 순차적으로 누적해 최종 분류를 수행하는 방식입니다.

이번 실험에서는 5개 클래스를 대상으로 학습했고, validation macro F1 0.8316, test macro F1 0.7627의 성능을 얻었습니다. 특히 수사기관 사칭형과 대출 사기형 같은 위험 범주에서 의미 있는 탐지 성능을 확인했습니다.

물론 아직 한계도 있습니다. 일부 클래스는 표현이 유사해 혼동이 있었고, validation 대비 test 성능이 다소 하락했습니다. 앞으로는 데이터 증강, 오분류 분석, 실시간 처리 최적화를 통해 실제 현장 적용 가능성을 높이고자 합니다.

저희 프로젝트의 핵심 가치는, 보이스피싱을 사후 분석하는 데서 그치지 않고 통화 중 즉시 위험을 인지하고 대응하도록 돕는다는 점입니다.

---

## 15. 포스터 레이아웃 제안

### 섹션 1. 문제 배경

- 보이스피싱 피해 심각성
- 기존 대응 방식의 한계
- 실시간 탐지 필요성

### 섹션 2. 시스템 구조

- 음성 입력
- STT
- 텍스트 분류
- 대응 가이드

### 섹션 3. 모델 구조

- KoELECTRA
- Attention Pooling
- Mamba SSM
- Classification Head

### 섹션 4. 실험 결과

- Validation / Test 지표
- 클래스별 F1
- best epoch

### 섹션 5. 기대 효과 및 향후 계획

- 실사용 가능성
- 한계
- 개선 방향

---

## 16. 포스터에 넣기 좋은 짧은 문구

- “실시간 통화 분석 기반 보이스피싱 탐지”
- “긴 통화 문맥을 반영하는 Mamba 기반 분류기”
- “탐지와 대응 가이드를 연결한 통합 데모”
- “사후 대응이 아닌 통화 중 예방”

