# Mamba 성능 개선 실험 계획서

> 작성일: 2026-05-13  
> **수정일: 2026-05-14** (수정2차 — freeze_init, diff_lr, short_window, sent_window 실험 완료 결과 반영)  
> 관련 실험 폴더: `models/classifier/experiments/`  
> 작성 배경: KoELECTRA+Mamba 결합 모델이 KoELECTRA 단독 모델 대비 성능이 낮아진 원인을 분석하고, 개선 실험을 체계적으로 수행하기 위한 계획서

---

## 0. 데이터셋 현황

| 구분 | 샘플 수 |
|------|---------|
| Train | 2,978건 |
| Validation | - |
| Test | 257건 |

### 대화 길이 통계 (train.csv 기준)

| 항목 | 평균 | 중간값 | 최소 | 최대 |
|------|------|--------|------|------|
| 문장 수 | 21.4 | 16 | 2 | 429 |
| 어절 수 | 177.6 | 138 | 12 | 4,941 |
| 글자 수 | 734.8 | 576 | 46 | 19,857 |
| 토큰 수 (추정) | 213~266 | 166~207 | 14~18 | 5,929~7,412 |

> **핵심**: 중간값 기준 ~166~207 토큰 → WINDOW_SIZE=128 적용 시 대부분 **1~2 세그먼트**만 생성됨.

---

## 1. 원인 진단: Mamba 결합 모델 성능 저하 분석

### 1-1. 시퀀스 부족 (주요 원인)
- 데이터 중간값(~166 토큰)을 WINDOW_SIZE=128로 자르면 윈도우가 **1~2개**에 불과
- Mamba는 긴 시퀀스(수십 개 이상 세그먼트)에서 상태 누적 효과가 발휘되는 SSM
- 2개 세그먼트로는 Mamba가 맥락을 누적할 기회가 없어 단순 MLP와 다름없음

### 1-2. Catastrophic Forgetting (가중치 붕괴)
- 학습이 완료된 KoELECTRA(수억 파라미터)와 무작위 초기화된 Mamba를 **동일 LR로 동시 학습**
- 초기 Mamba의 큰 오차(loss)가 역전파 시 KoELECTRA의 사전학습 지식을 덮어씀
- 결과적으로 두 모델 모두 불안정한 상태에서 수렴

### 1-3. 키워드 희석
- 피싱 대화의 핵심 키워드("대포통장", "공증", "공무원 사칭" 등)가
  Mamba의 선형 상태 압축 과정에서 일반 대화 맥락과 혼합
- 세그먼트가 1~2개일 때 Mamba 처리 이점이 사실상 없음

---

## 2. 전체 실험 결과 종합

### 2-1. 실험 결과 총괄 (Test Set, n=257)

| 실험명 | Accuracy | Macro F1 | Weighted F1 | 상태 |
|--------|----------|----------|-------------|------|
| `koelectra_phishing5` (KoELECTRA Baseline) | 0.7860 | 0.8146 | 0.7894 | ✅ 완료 |
| `hce_ordinal` L1 (KoELECTRA+Mamba L1) | 0.8366 | 0.8429 | 0.8378 | ✅ 완료 |
| `hce_ordinal` L2 (KoELECTRA+Mamba L2) | 0.7121 | 0.7470 | 0.7098 | ✅ 완료 |
| `freeze_init` (2단계 동결 학습) | **0.8560** | **0.8591** | **0.8565** | ✅ 완료 |
| `diff_lr` (차등 학습률 100배) | 0.7471 | 0.7686 | 0.7462 | ✅ 완료 |
| `short_window` (W=64, S=32) | 0.8016 | 0.8096 | 0.8003 | ✅ 완료 |
| `sent_window` (문장 단위 분할) | 0.7549 | 0.7830 | 0.7563 | ✅ 완료 |

> **최고 성능**: `freeze_init` — Macro F1 **0.8591** (Baseline 대비 +4.5%p)

---

## 3. 기존 실험 결과 (수정1차 기준)

### 클래스별 F1 상세 (Baseline vs Mamba L1)

| 클래스 | KoELECTRA Only | KoELECTRA+Mamba L1 | 차이 |
|--------|---------------|----------------------|------|
| 상품 가입 및 해지 | 0.774 | 0.855 | **+0.081** |
| 이체 출금 대출서비스 | 0.691 | 0.756 | **+0.065** |
| 잔고 및 거래내역 | 0.783 | 0.855 | **+0.072** |
| 수사기관 사칭형 | **0.928** | 0.904 | -0.024 |
| 대출 사기형 | **0.898** | 0.844 | -0.054 |

---

## 4. 실험 A: `koelectra_mamba_freeze_init` — 동결 & 초기화 ✅ 완료

**폴더**: `models/classifier/experiments/koelectra_mamba_freeze_init/`  
**상태**: ✅ 완료 (2026-05-13)

### 핵심 전략: 2단계 학습

```
Phase 1 — Epoch 1~3: KoELECTRA 완전 동결
  - KoELECTRA: requires_grad = False (gradient 완전 차단)
  - Mamba + Head만 학습: LR = 5e-4
  - 목적: Mamba가 KoELECTRA 표현 공간에 적응하도록 시간 부여

Phase 2 — Epoch 4~8: KoELECTRA 동결 해제
  - KoELECTRA: LR = 1e-5 (매우 낮게, 기존 2e-5의 절반)
  - Mamba + Head: LR = 5e-4 (유지)
  - 목적: 안정화된 Mamba와 함께 KoELECTRA 조심스럽게 미세조정
```

### 파라미터 설정

| 파라미터 | 값 | 기존(`hce_ordinal`) 대비 |
|---|---|---|
| `FREEZE_EPOCHS` | **3** | 신규 추가 |
| `ENCODER_LR` (2단계) | **1e-5** | `2e-5` → 절반 |
| `UPPER_LR` | **5e-4** | 동일 |
| `EPOCHS` | 8 | 동일 |
| `BATCH_SIZE` | 8 | 동일 |

### 테스트 결과 (n=257)

| 지표 | 값 |
|------|-----|
| **Accuracy** | **0.8560** |
| **Macro F1** | **0.8591** |
| **Weighted F1** | **0.8565** |
| Micro F1 | 0.8560 |

### 클래스별 성능

| 클래스 | F1 | Precision | Recall | Support |
|--------|----|-----------|--------|---------|
| 상품 가입 및 해지 | **0.8767** | 0.8889 | 0.8649 | 74 |
| 이체 출금 대출서비스 | **0.8189** | 0.7761 | 0.8667 | 60 |
| 잔고 및 거래내역 | **0.8387** | 0.8667 | 0.8125 | 64 |
| 수사기관 사칭형 | **0.9167** | 0.9167 | 0.9167 | 36 |
| 대출 사기형 | 0.8444 | 0.8636 | 0.8261 | 23 |

### 혼동 행렬

```
실제\예측   가입해지  이체출금  잔고거래  수사기관  대출사기
가입해지  [  64,     7,     3,     0,     0 ]
이체출금  [   4,    52,     4,     0,     0 ]
잔고거래  [   4,     8,    52,     0,     0 ]
수사기관  [   0,     0,     0,    33,     3 ]
대출사기  [   0,     0,     1,     3,    19 ]
```

### 분석
- **Baseline 대비 +4.5%p Macro F1 향상** (0.8146 → 0.8591)
- **hce_ordinal L1 대비 +1.6%p 추가 향상** (0.8429 → 0.8591)
- 이체출금 클래스: 0.756 (L1) → **0.819** (+6.3%p) — 가장 큰 개선 효과
- 수사기관 사칭형: 0.904 (L1) → **0.917** (+1.3%p) — 피싱 클래스도 동반 개선
- 동결 단계(Phase 1)에서 Mamba가 KoELECTRA 표현 공간에 충분히 적응한 뒤 협력 학습하여 전 클래스 개선

---

## 5. 실험 B: `koelectra_mamba_diff_lr` — 차등 학습률 ✅ 완료

**폴더**: `models/classifier/experiments/koelectra_mamba_diff_lr/`  
**상태**: ✅ 완료 (2026-05-13)

### 핵심 전략: LR 100배 격차

```
Epoch 1~8 (단일 단계, 동결 없음):
  KoELECTRA: LR = 1e-5  ← 사전학습 지식 보호
  Mamba + Head: LR = 1e-3  ← 무작위 초기화, 빠른 수렴
  격차: 100배 → Mamba가 주도적으로 학습, KoELECTRA는 조금씩만 업데이트
```

### 파라미터 설정

| 파라미터 | 값 | 기존(`hce_ordinal`) 대비 |
|---|---|---|
| `ENCODER_LR` | **1e-5** | `2e-5` → 절반 |
| `UPPER_LR` | **1e-3** | `5e-4` → **2배** |
| LR 비율 (Mamba/ENC) | **100배** | 25배 → 4배 증가 |
| 동결 여부 | 없음 | 없음 |

### 테스트 결과 (n=257)

| 지표 | 값 |
|------|-----|
| Accuracy | 0.7471 |
| Macro F1 | 0.7686 |
| Weighted F1 | 0.7462 |
| Micro F1 | 0.7471 |

### 클래스별 성능

| 클래스 | F1 | Precision | Recall | Support |
|--------|----|-----------|--------|---------|
| 상품 가입 및 해지 | 0.7742 | 0.7407 | 0.8108 | 74 |
| 이체 출금 대출서비스 | 0.5546 | 0.5593 | 0.5500 | 60 |
| 잔고 및 거래내역 | 0.7705 | 0.8103 | 0.7344 | 64 |
| 수사기관 사칭형 | 0.9067 | 0.8718 | 0.9444 | 36 |
| 대출 사기형 | 0.8372 | 0.9000 | 0.7826 | 23 |

### 혼동 행렬

```
실제\예측   가입해지  이체출금  잔고거래  수사기관  대출사기
가입해지  [  60,    12,     2,     0,     0 ]
이체출금  [  18,    33,     9,     0,     0 ]
잔고거래  [   3,    14,    47,     0,     0 ]
수사기관  [   0,     0,     0,    34,     2 ]
대출사기  [   0,     0,     0,     5,    18 ]
```

### 분석
- **Baseline(0.8146) 대비 오히려 하락** — Macro F1 0.7686 (-4.6%p)
- 이체출금 클래스 F1 **0.555**로 전 실험 중 최저 — 18건을 가입해지로 오분류
- LR 100배 격차도 초기 epoch KoELECTRA 일부 손상을 완전히 막지 못함
- **결론**: 암묵적 LR 보호는 명시적 gradient 차단(freeze_init)보다 효과 미흡

---

## 6. 실험 A vs B 비교

| 관점 | 실험 A (freeze_init) | 실험 B (diff_lr) |
|---|---|---|
| **KoELECTRA 보호** | 명시적 동결 (gradient 완전 차단) | 암묵적 보호 (LR 격차) |
| **초기 KoELECTRA 업데이트** | epoch 1~3: 전혀 없음 | epoch 1부터 `1e-5`로 조금씩 |
| **Mamba LR** | `5e-4` | `1e-3` (더 빠름) |
| **KoELECTRA LR** | epoch 4~부터 `1e-5` | 처음부터 `1e-5` |
| **테스트 Macro F1** | **0.8591** | 0.7686 |
| **Baseline 대비** | **+4.5%p** | **-4.6%p** |
| **결과** | ✅ 최고 성능 달성 | ❌ Baseline 미달 |

> **결론**: Catastrophic Forgetting 방지에는 명시적 gradient 차단이 LR 격차보다 훨씬 효과적.  
> diff_lr 방식의 경우 초기 epoch에서도 KoELECTRA 파라미터가 Mamba의 큰 오차에 의해 일부 손상되어 복구 불가.

---

## 7. 실험 C: `koelectra_mamba_short_window` — 짧은 고정 윈도우 ✅ 완료

**폴더**: `models/classifier/experiments/koelectra_mamba_short_window/`  
**상태**: ✅ 완료 (2026-05-13)

### 핵심 전략

```
WINDOW_SIZE: 128 → 64 토큰
STRIDE:      100 → 32 토큰 (50% 중첩)

세그먼트 수 변화 (중간값 ~166 토큰 기준):
  현재:  (166 - 2) / (128 - 100) ≈ 약 2개
  변경:  (166 - 2) / (64 - 32)   ≈ 약 5개 (약 3배 증가)
```

### 파라미터 설정

| 파라미터 | 현재 (`hce_ordinal`) | 변경 | 기대 효과 |
|---|---|---|---|
| `WINDOW_SIZE` | 128 | **64** | 세그먼트 수 약 3배 증가 |
| `STRIDE` | 100 | **32** | 세그먼트 간 50% 중첩 |
| `BATCH_SIZE` | 8 | **4** | 메모리 증가 대응 |
| `ENCODER_LR` | 2e-5 | **2e-5** | 동일 |
| `UPPER_LR` | 5e-4 | **5e-4** | 동일 |

### 테스트 결과 (n=257)

| 지표 | 값 |
|------|-----|
| Accuracy | 0.8016 |
| Macro F1 | 0.8096 |
| Weighted F1 | 0.8003 |
| Micro F1 | 0.8016 |

### 클래스별 성능

| 클래스 | F1 | Precision | Recall | Support |
|--------|----|-----------|--------|---------|
| 상품 가입 및 해지 | 0.8199 | 0.7586 | 0.8919 | 74 |
| 이체 출금 대출서비스 | 0.7018 | 0.7407 | 0.6667 | 60 |
| 잔고 및 거래내역 | 0.8167 | 0.8750 | 0.7656 | 64 |
| 수사기관 사칭형 | 0.8696 | 0.9091 | 0.8333 | 36 |
| 대출 사기형 | 0.8400 | 0.7778 | 0.9130 | 23 |

### 혼동 행렬

```
실제\예측   가입해지  이체출금  잔고거래  수사기관  대출사기
가입해지  [  66,     6,     2,     0,     0 ]
이체출금  [  15,    40,     5,     0,     0 ]
잔고거래  [   6,     8,    49,     1,     0 ]
수사기관  [   0,     0,     0,    30,     6 ]
대출사기  [   0,     0,     0,     2,    21 ]
```

### 분석
- Baseline(0.8146) 대비 소폭 하락 — Macro F1 0.8096 (-0.5%p)
- 이체출금 클래스: Baseline 0.691 → **0.702** (+1.1%p) — 세그먼트 증가 효과 일부 확인
- **수사기관 사칭형 F1 급락**: 0.928 (Baseline) → **0.870** (-5.8%p) — 6건이 대출사기로 오분류
- 짧은 윈도우로 인해 피싱 핵심 키워드가 여러 세그먼트에 분산되어 인식률 저하 가능성
- **결론**: 세그먼트 수 증가만으로는 학습 전략 문제(Catastrophic Forgetting)를 해결하지 못함

---

## 8. 실험 D: `koelectra_mamba_sent_window` — 문장 단위 동적 윈도우 ✅ 완료

**폴더**: `models/classifier/experiments/koelectra_mamba_sent_window/`  
**상태**: ✅ 완료 (2026-05-13)

### 핵심 전략

```
현재: 토큰 수 기준 고정 분할 → 단어/의미 단위가 경계에서 잘림
변경: 문장(마침표/화자교대) 기준 의미 단위 분할 → 의미 완결 유지

예시:
  현재: ["안녕하세요 저희는 대출"] ["팀입니다 고객님 혹시"]  ← 문장이 잘림
  변경: ["안녕하세요 저희는 대출팀입니다."] ["고객님 혹시 대포통장이..."]
```

### 파라미터 설정

| 파라미터 | 값 |
|---|---|
| `WINDOW_SIZE` | 128 (max tokens per segment, sentence-aware) |
| `STRIDE` | None (문장 경계 기반, 고정 stride 없음) |
| `BATCH_SIZE` | **4** (가변 세그먼트 수 대응) |
| `ENCODER_LR` | 2e-5 |
| `UPPER_LR` | 5e-4 |

### 테스트 결과 (n=257)

| 지표 | 값 |
|------|-----|
| Accuracy | 0.7549 |
| Macro F1 | 0.7830 |
| Weighted F1 | 0.7563 |
| Micro F1 | 0.7549 |

### 클래스별 성능

| 클래스 | F1 | Precision | Recall | Support |
|--------|----|-----------|--------|---------|
| 상품 가입 및 해지 | 0.7600 | 0.7500 | 0.7703 | 74 |
| 이체 출금 대출서비스 | 0.5854 | 0.5714 | 0.6000 | 60 |
| 잔고 및 거래내역 | 0.7805 | 0.8136 | 0.7500 | 64 |
| 수사기관 사칭형 | **0.9143** | 0.9412 | 0.8889 | 36 |
| 대출 사기형 | **0.8750** | 0.8400 | 0.9130 | 23 |

### 혼동 행렬

```
실제\예측   가입해지  이체출금  잔고거래  수사기관  대출사기
가입해지  [  57,    12,     5,     0,     0 ]
이체출금  [  18,    36,     6,     0,     0 ]
잔고거래  [   1,    15,    48,     0,     0 ]
수사기관  [   0,     0,     0,    32,     4 ]
대출사기  [   0,     0,     0,     2,    21 ]
```

### 분석
- Baseline(0.8146) 대비 하락 — Macro F1 0.7830 (-3.2%p)
- 피싱 클래스는 상대적으로 준수: 수사기관 0.914, 대출사기 0.875
- **이체출금 클래스 최악** (0.585): 18건이 가입해지로 오분류 — 문장 경계 분할이 도리어 맥락 흐름을 끊어 이 클래스 판별에 악영향
- 가변 세그먼트 수로 인한 배치 불균일성이 학습 안정성에 영향 가능성
- **결론**: 의미 단위 보존만으로는 성능 개선 불충분 — 학습 전략(Catastrophic Forgetting 방지) 병행 필요

---

## 9. 4개 신규 실험 비교 분석

> **비교 기준**: `hce_ordinal L1` (KoELECTRA+Mamba L1, HCE+Ordinal 손실, 기존 최고 성능)  
> Accuracy=0.8366 / Macro F1=0.8429 / Weighted F1=0.8378

### 9-1. 전체 지표 비교

| 실험 | Accuracy | Macro F1 | Weighted F1 | hce_ordinal L1 대비 Macro F1 |
|------|----------|----------|-------------|------------------------------|
| `hce_ordinal L1` *(기준)* | 0.8366 | 0.8429 | 0.8378 | — |
| **`freeze_init`** | **0.8560** | **0.8591** | **0.8565** | **+1.6%p** ✅ |
| `short_window` | 0.8016 | 0.8096 | 0.8003 | -3.3%p ❌ |
| `sent_window` | 0.7549 | 0.7830 | 0.7563 | -6.0%p ❌ |
| `diff_lr` | 0.7471 | 0.7686 | 0.7462 | -7.4%p ❌ |

### 9-2. 클래스별 F1 비교 — hce_ordinal L1 기준

| 클래스 | hce_ordinal L1 *(기준)* | freeze_init | short_window | sent_window | diff_lr |
|--------|------------------------|-------------|--------------|-------------|---------|
| 상품 가입 및 해지 | 0.8552 | **0.8767** (+2.2%p) | 0.8199 (-3.5%p) | 0.7600 (-9.5%p) | 0.7742 (-8.1%p) |
| 이체 출금 대출서비스 | 0.7559 | **0.8189** (+6.3%p) | 0.7018 (-5.4%p) | 0.5854 (-17.1%p) | 0.5546 (-20.1%p) |
| 잔고 및 거래내역 | 0.8548 | 0.8387 (-1.6%p) | 0.8167 (-3.8%p) | 0.7805 (-7.4%p) | 0.7705 (-8.4%p) |
| 수사기관 사칭형 | 0.9041 | **0.9167** (+1.3%p) | 0.8696 (-3.5%p) | 0.9143 (+1.0%p) | 0.9067 (+0.3%p) |
| 대출 사기형 | 0.8444 | 0.8444 (0.0%p) | 0.8400 (-0.4%p) | **0.8750** (+3.1%p) | 0.8372 (-0.9%p) |

### 9-3. hce_ordinal L1 대비 클래스별 등락 요약

| 클래스 | freeze_init | short_window | sent_window | diff_lr |
|--------|:-----------:|:------------:|:-----------:|:-------:|
| 상품 가입 및 해지 | ▲ +2.2%p | ▼ -3.5%p | ▼ -9.5%p | ▼ -8.1%p |
| 이체 출금 대출서비스 | ▲ **+6.3%p** | ▼ -5.4%p | ▼ -17.1%p | ▼ -20.1%p |
| 잔고 및 거래내역 | ▼ -1.6%p | ▼ -3.8%p | ▼ -7.4%p | ▼ -8.4%p |
| 수사기관 사칭형 | ▲ +1.3%p | ▼ -3.5%p | ▲ +1.0%p | ▲ +0.3%p |
| 대출 사기형 | → 0.0%p | ▼ -0.4%p | ▲ +3.1%p | ▼ -0.9%p |
| **Macro F1** | **▲ +1.6%p** | **▼ -3.3%p** | **▼ -6.0%p** | **▼ -7.4%p** |

### 9-4. 전략별 효과 정리

| 전략 | 실험 | L1 대비 결과 | 핵심 원인 |
|------|------|------------|-----------|
| 명시적 동결 (gradient 차단) | freeze_init | ✅ **+1.6%p** | Catastrophic Forgetting 완전 차단, 이체출금 대폭 개선 |
| LR 격차 100배 (암묵적 보호) | diff_lr | ❌ -7.4%p | 초기 epoch KoELECTRA 손상 여전히 발생 |
| 짧은 고정 윈도우 (세그먼트 3배) | short_window | ❌ -3.3%p | 세그먼트 증가 효과 일부, 피싱 클래스 혼동 증가 |
| 문장 단위 의미 분할 | sent_window | ❌ -6.0%p | 학습 전략 문제 미해결, 이체출금 오분류 심각 |

---

## 10. 전체 실험 로드맵 (업데이트)

```
[완료] ─────────────────────────────────────────────────────────────────
  ① koelectra_phishing5          Macro F1 = 0.8146  ← 기준선
  ② hce_ordinal (Mamba L1)       Macro F1 = 0.8429  ← Mamba L1 성공
  ③ hce_ordinal (Mamba L2)       Macro F1 = 0.7470  ← 깊어질수록 급격한 하락
  ④ hce_ordinal (Mamba L4/L6)    NaN 폭발           ← 모델 붕괴

[완료: 학습 전략 최적화] ──────────────────────────────────────────────
  ⑤ koelectra_mamba_freeze_init  Macro F1 = 0.8591  ✅ 현재 최고 성능
  ⑥ koelectra_mamba_diff_lr      Macro F1 = 0.7686  ❌ Baseline 미달

[완료: 전처리 개선] ──────────────────────────────────────────────────
  ⑦ koelectra_mamba_short_window  Macro F1 = 0.8096  ⚠️ Baseline 소폭 미달
  ⑧ koelectra_mamba_sent_window   Macro F1 = 0.7830  ❌ Baseline 미달

[목표 달성 현황] ─────────────────────────────────────────────────────
  목표: Mamba 결합 모델 Macro F1 0.87 이상
  현재 최고: freeze_init 0.8591 (목표 대비 -1.1%p)
```

---

## 11. 핵심 인사이트 및 향후 방향

### 11-1. 핵심 발견사항

1. **Catastrophic Forgetting 방지가 핵심**: freeze_init이 유일하게 Baseline을 크게 상회
2. **암묵적 보호(diff_lr)의 한계 확인**: LR 격차만으로는 초기 손상 방지 불충분
3. **세그먼트 수 단독 증가의 한계**: short_window, sent_window 모두 학습 전략 문제 미해결 시 효과 미미
4. **이체출금 클래스가 핵심 난제**: 전 실험에서 가장 낮은 F1, 특히 가입해지 클래스와의 혼동이 심각

### 11-2. 향후 실험 방향

| 우선순위 | 실험 아이디어 | 기대 효과 |
|----------|--------------|-----------|
| ★★★ | **freeze_init + short_window 결합** | 동결 전략 + 세그먼트 증가 시너지 |
| ★★★ | **freeze_init + sent_window 결합** | 동결 전략 + 의미 보존 시너지 |
| ★★ | L2 + freeze_init | 더 깊은 Mamba + 안정 학습 |
| ★★ | freeze_init 동결 에폭 수 조정 (2 vs 4) | 최적 Phase 1 길이 탐색 |
| ★ | 이체출금 클래스 데이터 증강 | 가장 취약한 클래스 집중 강화 |

---

## 12. 성능 목표 및 현황

| 지표 | 기준선 (Baseline) | 현재 최고 (freeze_init) | 목표 |
|------|-------------------|------------------------|------|
| Macro F1 | 0.8146 | **0.8591** | **0.87 이상** |
| 피싱 클래스 F1 평균 | 0.913 | 0.881 | 0.94 이상 |
| 일반 클래스 F1 평균 | 0.749 | 0.845 | 0.82 이상 |

> **일반 클래스 목표 달성**: freeze_init으로 일반 클래스 F1 0.845 → 목표(0.82) 초과 달성  
> **피싱 클래스는 소폭 하락**: freeze_init에서 수사기관 0.917, 대출사기 0.844 — 추가 개선 여지 있음

---

## 13. 파일 위치 요약

```
models/classifier/experiments/
├── koelectra_phishing5/              ✅ 완료 (기준선, Macro F1 0.8146)
├── koelectra_mamba_phishing5/        ✅ 완료
├── koelectra_mamba_hce_ordinal/      ✅ 완료 (HCE+Ordinal, L1 최고: 0.8429)
├── koelectra_mamba_freeze_init/      ✅ 완료 (★ 현재 최고 성능: 0.8591)
│   ├── config.py   FREEZE_EPOCHS=3, ENCODER_LR=1e-5, UPPER_LR=5e-4
│   ├── train.py    2단계 학습 (phase_change 로직 포함)
│   ├── evaluate.py
│   ├── logs/koelectra_mamba_freeze_init_20260513_152612_test_20260513_154647_eval.json
│   └── checkpoints/koelectra_mamba_freeze_init_latest.json
├── koelectra_mamba_diff_lr/          ✅ 완료 (Macro F1 0.7686, Baseline 미달)
│   ├── config.py   ENCODER_LR=1e-5, UPPER_LR=1e-3
│   ├── logs/koelectra_mamba_diff_lr_20260513_141957_test_20260513_152453_eval.json
│   └── checkpoints/koelectra_mamba_diff_lr_latest.json
├── koelectra_mamba_short_window/     ✅ 완료 (Macro F1 0.8096, Baseline 소폭 미달)
│   ├── config.py   WINDOW_SIZE=64, STRIDE=32, BATCH_SIZE=4
│   ├── logs/koelectra_mamba_short_window_20260513_161431_test_20260513_164432_eval.json
│   └── checkpoints/koelectra_mamba_short_window_latest.json
└── koelectra_mamba_sent_window/      ✅ 완료 (Macro F1 0.7830, Baseline 미달)
    ├── config.py   문장 단위 분할, BATCH_SIZE=4
    ├── logs/koelectra_mamba_sent_window_20260513_164525_test_20260513_165801_eval.json
    └── checkpoints/koelectra_mamba_sent_window_latest.json
```
