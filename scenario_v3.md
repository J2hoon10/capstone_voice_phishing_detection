# 캡스톤디자인 데모데이 포스터 발표 대본 Ver.3

## 1. 작품명

**AIShield: 실시간 음성 보이스피싱 탐지 및 대응 가이드 시스템**

부제:
통화 중 대화 문맥을 분석해 보이스피싱 위험을 탐지하고, 즉시 대응 가이드를 제공하는 AI 시스템

---

## 2. 포스터용 핵심 소개문

보이스피싱은 더 이상 단순한 스팸 전화 문제가 아니다.  
최근 보이스피싱은 정상 번호 도용, 기관 사칭, 심리적 압박, 딥페이크 음성 등으로 빠르게 고도화되고 있다.  
기존의 번호 차단이나 URL 탐지 방식만으로는 이러한 범죄를 막기 어렵다.

저희 팀은 이 문제를 해결하기 위해, 통화 음성을 실시간으로 텍스트화한 뒤 대화의 문맥을 분석하여 보이스피싱 위험을 판단하고, 사용자에게 즉시 경고와 대응 가이드를 제공하는 시스템을 개발했다.

---

## 3. 왜 필요한가

이 프로젝트가 필요한 이유는 크게 세 가지이다.

- 첫째, 보이스피싱 피해는 통화가 끝난 뒤가 아니라 통화 도중 발생하는 경우가 많다.
- 둘째, 기존 시스템은 번호나 키워드 같은 고정 패턴에는 강하지만 대화의 의미와 흐름은 이해하지 못한다.
- 셋째, 피해가 고령층과 디지털 취약계층에 집중되는 만큼, 실시간으로 위험을 알려주는 보조 시스템이 필요하다.

즉, 핵심은 사후 분석이 아니라 **통화 중 실시간 개입**이다.

---

## 4. 문제 정의

본 프로젝트는 다음 문제를 해결하는 것을 목표로 한다.

- 통화 내용을 실시간으로 이해할 수 있는가
- 긴 대화 문맥 속에서 피싱 위험을 조기에 감지할 수 있는가
- 단순 탐지에 그치지 않고 유형별 대응까지 연결할 수 있는가
- 개인정보 유출 부담을 줄이는 구조로 구현할 수 있는가

---

## 5. 시스템 구조

시스템은 다음 흐름으로 동작한다.

`음성 입력 -> STT 변환 -> 텍스트 분류 -> 위험 유형 판단 -> 대응 가이드 제공`

구성 요소는 다음과 같다.

- STT 모듈: 음성을 텍스트로 변환
- 분류 모델: 통화 문맥을 바탕으로 피싱 유형 판단
- 가이드 모듈: 예측된 유형에 맞는 대응 문구 제공
- 백엔드/프론트엔드: 결과 시각화 및 데모 인터페이스 제공

---

## 6. 모델 설계

보이스피싱 통화는 길고, 위험 문장이 후반부에 등장하는 경우가 많다.  
그래서 본 프로젝트는 텍스트를 한 번에 처리하지 않고 여러 세그먼트로 나눈 뒤, 순차적으로 해석하는 구조를 사용했다.

모델 구조는 다음과 같다.

- KoELECTRA로 각 세그먼트 인코딩
- Attention-weighted pooling으로 핵심 정보 압축
- Mamba SSM으로 세그먼트 간 문맥 누적
- 분류 헤드에서 최종 유형 예측

이 구조를 통해 긴 대화에서도 흐름을 따라가며 위험도를 점진적으로 판단할 수 있다.

---

## 7. 분류 클래스

이번 실험에서는 다음 5개 클래스를 대상으로 학습하였다.

- 상품 가입 및 해지
- 이체 출금 대출서비스
- 잔고 및 거래내역
- 수사기관 사칭형
- 대출 사기형

이 구성은 단순히 피싱 여부만 판단하는 것이 아니라, **유형에 따라 다른 대응 가이드를 연결하기 위한 설계**라는 데 의미가 있다.

---

## 8. 실험 설정

- 학습 데이터: `train.csv`
- 검증 데이터: `val.csv`
- 테스트 데이터: `test.csv`
- 배치 크기: 8
- 학습 epoch: 8
- 모델 선택 기준: validation macro F1

가장 좋은 성능은 **epoch 6**에서 나왔고, 해당 체크포인트를 최종 테스트에 사용했다.

---

## 9. 실험 결과

### Validation 최고 성능

- Accuracy: `0.8484`
- Macro F1: `0.8316`
- Weighted F1: `0.8484`

### Test 성능

- Accuracy: `0.7607`
- Macro F1: `0.7627`
- Weighted F1: `0.7616`

### 클래스별 Test F1

- 상품 가입 및 해지: `0.8117`
- 이체 출금 대출서비스: `0.6286`
- 잔고 및 거래내역: `0.7869`
- 수사기관 사칭형: `0.8551`
- 대출 사기형: `0.7312`

---

## 10. 결과 해석

- `수사기관 사칭형`에서 가장 높은 성능을 보여, 대표적인 고위험 시나리오 탐지 가능성을 확인했다.
- `대출 사기형`도 비교적 안정적으로 탐지되었다.
- `이체 출금 대출서비스`는 정상 금융 대화와 표현이 겹쳐 상대적으로 어려운 클래스였다.
- validation 대비 test 점수가 다소 하락해, 일반화 성능 개선이 앞으로의 주요 과제로 보인다.

즉, 현재 결과는 **실제 데모 시연에는 충분히 의미 있는 수준**이지만, 상용화를 위해서는 데이터와 추론 안정성을 더 보완해야 한다.

---

## 11. 기대 효과

- 통화 중 위험 상황을 빠르게 인지시켜 피해를 줄일 수 있다.
- 고령층과 디지털 취약계층을 위한 실시간 보조 도구로 활용 가능하다.
- 탐지 결과와 대응 가이드를 연결해 사용자 행동 변화를 유도할 수 있다.
- 장기적으로는 온디바이스 보안 AI 서비스로 확장 가능하다.

---

## 12. 한계와 향후 계획

현재 한계는 다음과 같다.

- 테스트 성능이 검증 성능보다 낮다.
- 일부 클래스 간 의미 중첩으로 오분류가 발생한다.
- 실제 통화 잡음, 방언, 화자 분리 등 현장 조건 검증이 부족하다.

향후에는 다음 방향으로 개선할 계획이다.

- 데이터 증강 및 클래스 불균형 완화
- 오분류 분석 기반 모델 개선
- 실시간 latency 최적화
- 조기 탐지 성능 강화
- 모델 경량화 및 온디바이스 적용 검증

---

## 13. 포스터용 한 줄 메시지

**“보이스피싱은 번호가 아니라 대화의 문맥으로 진화하고 있다.  
AIShield는 통화 중 그 문맥을 읽고, 위험을 감지하며, 즉시 대응을 돕는다.”**

---

## 14. 1분 발표 대본

안녕하세요. 저희 팀은 실시간 음성 보이스피싱 탐지 및 대응 가이드 시스템, AIShield를 개발했습니다.

기존의 보이스피싱 차단 방식은 주로 발신 번호나 문자 내 URL 같은 고정 패턴에 의존합니다. 하지만 최근 보이스피싱은 정상 번호를 도용하고, 대화의 흐름 속에서 피해자의 심리를 압박하는 방식으로 진화하고 있기 때문에 이런 방식만으로는 한계가 있습니다.

저희는 이 문제를 해결하기 위해 통화 음성을 텍스트로 변환한 뒤, KoELECTRA와 Mamba 기반 분류 모델로 긴 대화 문맥을 분석하는 구조를 설계했습니다. 그리고 단순히 피싱 여부만 예측하는 것이 아니라, 피싱 유형에 따라 대응 가이드까지 함께 제공하도록 구성했습니다.

실험 결과 validation macro F1은 0.8316, test macro F1은 0.7627을 기록했고, 특히 수사기관 사칭형 탐지에서 높은 성능을 확인했습니다. 저희 프로젝트의 핵심 가치는 보이스피싱을 사후 분석하는 것이 아니라, 통화 중 위험을 감지하고 바로 대응하도록 돕는 데 있습니다.

---

## 15. 3분 발표 대본

안녕하세요. 저희 팀의 프로젝트는 AIShield, 즉 실시간 음성 보이스피싱 탐지 및 대응 가이드 시스템입니다.

저희가 이 주제를 선택한 이유는 기존 보이스피싱 대응 방식의 한계 때문입니다. 지금까지의 차단 시스템은 주로 전화번호 블랙리스트나 URL 탐지처럼 정적인 패턴에 의존해 왔습니다. 하지만 실제 보이스피싱은 정상 번호 도용, 기관 사칭, 심리적 압박 같은 방식으로 이루어지기 때문에, 결국 중요한 것은 번호가 아니라 대화의 문맥입니다.

또한 피해는 통화가 끝난 뒤가 아니라 통화 도중 발생합니다. 상대가 계좌 동결, 수사기관 조사, 가족 사고 같은 위기 상황을 연출하면 피해자는 순간적으로 판단력이 흔들릴 수 있습니다. 그래서 저희는 사후 분석이 아니라 통화 중 개입할 수 있는 시스템이 필요하다고 보았습니다.

저희 시스템은 먼저 음성을 STT로 텍스트화합니다. 이후 KoELECTRA 기반 인코더가 텍스트 세그먼트를 이해하고, Mamba SSM이 긴 대화 흐름을 따라가며 문맥을 누적합니다. 마지막으로 분류 헤드가 피싱 유형을 예측하고, 그 결과에 맞는 대응 가이드를 사용자에게 제공합니다.

이번 실험에서는 5개 클래스를 기준으로 학습을 진행했고, validation macro F1 0.8316, test macro F1 0.7627의 결과를 얻었습니다. 특히 수사기관 사칭형과 대출 사기형 같은 고위험 유형에서 비교적 안정적인 탐지 성능을 확인했습니다.

물론 아직 한계도 있습니다. 일부 클래스는 표현이 유사해 혼동이 있었고, validation 대비 test 성능도 다소 낮아졌습니다. 앞으로는 데이터 증강, 오분류 분석, 실시간 latency 최적화, 경량화 및 온디바이스 적용을 통해 실제 서비스 수준으로 발전시키는 것이 목표입니다.

저희 프로젝트의 핵심은 단순 분류 모델이 아닙니다. 보이스피싱을 통화가 끝난 뒤에 판단하는 것이 아니라, 통화 중 위험을 감지하고 사용자가 바로 대응할 수 있도록 돕는 보호형 AI 시스템이라는 데 의미가 있습니다.

---

## 16. 포스터 배치 추천

### 섹션 1. 문제 배경

- 보이스피싱 고도화
- 기존 대응 방식의 한계
- 실시간 개입 필요성

### 섹션 2. 시스템 구조

- 음성 입력
- STT
- 문맥 기반 분류
- 대응 가이드

### 섹션 3. 모델 구조

- KoELECTRA
- Attention Pooling
- Mamba SSM
- Classification Head

### 섹션 4. 실험 결과

- Validation / Test 성능
- 클래스별 F1
- Best epoch

### 섹션 5. 기대 효과 및 향후 계획

- 범죄 예방
- 취약계층 보호
- 온디바이스 확장성
- 데이터/모델 개선 계획

